# Copy this file to .env and fill in the values before running docker compose.

DB_USER=postgres
DB_PASSWORD=change_me_before_deploy
DB_NAME=whitelist_system

# Port on the host that maps to the app container (default 8080)
APP_PORT=8080
